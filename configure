#!/bin/bash

# Install rust if not present 
if ! command -v rustc &>/dev/null; then
	curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
fi
sudo apt-get update
sudo apt-get install -y libevdev-dev libudev-dev libinput-dev
